import user
import computer
import port
class menu
    def menu_show
        print ( "welcome to the penetration game")
        choice = input( "chose option-  1:see network 2:add compter ")
        if(choice == 1)
            show_values.show_computers(computers)
        else if (choice == 2)
            assign_values.assign_computer(computers,check_compadd)
        
