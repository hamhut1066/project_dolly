import user
import computer
import ports

class assign_values 

    def assign_user (self,user)
            user.name = input("please enter you name")
            

    def assign_computer (self,computer[10],check_compadd) #check_compadd returns the number of computer a user can add
            if (check_compadd == 0)
                break;
            else
                assign_computer = input ( "enter computer name")
                check_compadd = check_compadd -1
                
                    
            

    def assign_port(self, ports,check_portadd) #check_portadd checks the number of ports a user can add
             if (check_portadd == 0)
                break;
             else
                assign_port = input ( "enter computer name")
                check_portadd = check_portadd -1
